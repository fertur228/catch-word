# TODOS

Отложенный скоуп из ревью плана «движок v2» (/autoplan 11.07.2026, docs/PLAN-engine-v2.md).

- [ ] **Юнит-раннер для чистых функций** (P2, S) — в репо нет юнит-инфраструктуры (только
  Playwright/smoke). Завести vitest (или node:test+tsx) и покрыть: checkDictation/
  normalizeAnswer/stripDiacritics, buildWorkoutQuiz, маппинг score→SRS, validateExercises
  (вынести из quest-agent/index.ts в модуль + deno test). Из Э2/Э3: код написан, тесты — долг.
- [ ] **GOOGLE_API_KEY (AI Studio)** (P2, S) — завести ключ и переключить embed-cards с
  gte-small (384, англоцентричен) на gemini-embedding (768, мультиязычный: de/es/zh);
  пересчёт: truncate card_embeddings → джоб добьёт сам. Сейчас работает на gte-small.
- [ ] **Подписаться на алерты**: приложение ntfy (или https://ntfy.sh в браузере) → топик
  `takeword-agent-fertur228` — туда приходит «ночной агент не в порядке» (00:30 UTC).
- [ ] **Старые e2e-спеки устарели по локали** (P2, S) — paywall.spec/public.spec ждут русских
  плейсхолдеров («Введите email»), а дефолт приложения — английский (12 падений, НЕ регрессия:
  падают и на проде). Перевести селекторы на EN (как tests/e2e/review-engine.spec.ts) или
  научить приложение уважать локаль браузера. Также baseURL по умолчанию указывает на лендинг
  catch-words.com вместо app.catch-words.com (сплит доменов позже конфига).

- [ ] **Экран «Ночной отчёт тренера» для пользователя** (P3, M) — прозрачность: что агент
  делал ночью (двигал повторения, собирал тренировку). Why: доверие + вау-эффект. Депендс:
  edge-endpoint к agent_runs (RLS закрывает прямой доступ).
- [ ] **agent_health на лендинг («как это работает»)** (P3, S) — публичная страница с
  обезличенной статистикой ночных прогонов. Why: маркетинг технической глубины.
- [ ] **/gstack-design-consultation → DESIGN.md** (P2, S) — дизайн-системы нет, ревью шло по
  универсальным принципам; после демо зафиксировать токены/типографику/паттерны.
- [ ] **Глубокая a11y новых режимов** (P3, M) — скринридеры для открытых ответов (aria-live
  на вердикт), полная клавиатурная навигация. Базовые вещи (44px, контраст, Enter-submit)
  уже в плане (B.7).
- [ ] **Мультимодальная сверка «текст соответствует фото»** (P3, M) — v2 режима «Расскажи о
  фото»: слать фото+транскрипт в Gemini. Why: честная проверка описания. Cons: цена/латентность.
- [ ] **FSRS per-user оптимизация параметров** (P3, L) — после накопления review_events
  (месяцы данных); ночной job подбора параметров на истории юзера. Депендс: Э7 + iOS-паритет.
