// Роут «Новый пароль» (код + новый пароль). Реализация — в src/screens/reset-password-screen.tsx.
import { ResetPasswordScreen } from '@/screens/reset-password-screen';

export default ResetPasswordScreen;
