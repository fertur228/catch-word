// Роут промежуточного экрана «Сканирование» (камера → /scanning → /result).
// Реализация — в src/screens/scanning-screen.tsx (пишется отдельно).
import { ScanningScreen } from '@/screens/scanning-screen';

export default ScanningScreen;
