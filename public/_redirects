# Cloudflare Pages routing для статического экспорта Expo.
# Реальные HTML-страницы (лендинг, основные роуты) отдаются напрямую (SEO),
# а всё остальное — клиентская навигация и динамический /card/:id — на SPA-шелл.
/card/*   /index.html   200
/*        /index.html   200
